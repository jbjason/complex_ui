import 'package:flutter/material.dart';

class DocD extends StatelessWidget {
  const DocD({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
